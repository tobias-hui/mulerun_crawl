# Neon PostgreSQL 数据库配置（推荐）
# 从 Neon 控制台复制连接字符串，格式如下：
# postgresql://username:password@host/database?sslmode=require
DATABASE_URL=postgresql://username:password@host.neon.tech/database?sslmode=require

# 或者使用传统配置方式（如果未设置 DATABASE_URL）
# DB_HOST=localhost
# DB_PORT=5432
# DB_DATABASE=mulerun_crawl
# DB_USER=postgres
# DB_PASSWORD=your_password_here

# 可选：爬虫配置覆盖
# CRAWLER_HEADLESS=true
# CRAWLER_SCROLL_DELAY=2

# API Key 认证（可选，如果未设置则不启用验证）
# 设置后，所有需要认证的接口都需要在请求头中添加: X-API-Key: your_api_key_here
# API_KEY=your_secret_api_key_here

# 飞书 Webhook URL（可选，用于发送通知）
# 如果未设置，则不会发送飞书通知
# 默认使用提供的 Webhook URL，如需更换请修改
FEISHU_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/94adca4b-556b-4a5b-9b63-ce3cac5bd8bc
